#include "main.h"
/**
 * _islower -> checks if the character is lowercase
 * @c: character arguement
 * Return:  1 is returned or 0 depending on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
