#include "main.h"
/**
 * main - starting point
 * _islower -> checks to see if all characters are in lowercase
 * Return: return 1 or 0 depending on the condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
